# Provider-independent tea definition reconciliation

The TeaCatalogData tooling needs a reproducible, read-only reconciliation of
the full TheTea field inventory and the existing tea category tree before any
definition or product synchronization is prepared. Legacy `SPEC-TT-*` values
remain evidence while canonical candidates use the provider-independent
`SPEC-TEA-*` namespace.
