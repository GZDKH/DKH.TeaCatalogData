# Full TheTea locale policy

The source registry exposes 72 languages while DKH stores concrete cultures.
The synchronization plan needs an explicit source-to-destination policy that
supports one-to-many inheritance, regional overrides and script review without
calling inherited text native.
